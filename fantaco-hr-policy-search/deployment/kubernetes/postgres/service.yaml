apiVersion: v1
kind: Service
metadata:
  name: fantaco-hr-policy-search-db
  labels:
    app: fantaco-hr-policy-search-db
spec:
  type: ClusterIP
  selector:
    app: fantaco-hr-policy-search-db
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgresql
