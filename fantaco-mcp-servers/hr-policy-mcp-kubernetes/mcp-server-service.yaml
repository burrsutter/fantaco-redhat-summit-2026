apiVersion: v1
kind: Service
metadata:
  name: mcp-hr-policy-service
  labels:
    app: mcp-hr-policy
spec:
  type: ClusterIP
  selector:
    app: mcp-hr-policy
  ports:
  - port: 9007
    targetPort: 9007
    protocol: TCP
    name: http
