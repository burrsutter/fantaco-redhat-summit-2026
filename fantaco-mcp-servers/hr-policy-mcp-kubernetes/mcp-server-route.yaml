apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mcp-hr-policy-route
  labels:
    app: mcp-hr-policy
spec:
  to:
    kind: Service
    name: mcp-hr-policy-service
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
