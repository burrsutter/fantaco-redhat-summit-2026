apiVersion: v1
kind: Service
metadata:
  name: mcp-sales-order-service
  labels:
    app: mcp-sales-order
spec:
  type: ClusterIP
  selector:
    app: mcp-sales-order
  ports:
  - port: 9003
    targetPort: 9003
    protocol: TCP
    name: http
