apiVersion: v1
kind: Service
metadata:
  name: postgres-sord
  labels:
    app: postgres-sord
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: postgres-sord
      protocol: TCP
      name: postgres-sord
  selector:
    app: postgresql-sales-order
