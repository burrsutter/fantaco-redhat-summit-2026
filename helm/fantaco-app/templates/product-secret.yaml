{{- if .Values.product.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: fantaco-product-secret
type: Opaque
stringData:
  database.password: {{ .Values.product.database.password | quote }}
{{- end }}
