{{- if and .Values.hr.enabled .Values.hr.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.hr.route.name }}
  labels:
    app: {{ .Values.hr.name }}
spec:
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: {{ .Values.hr.route.tls.insecureEdgeTerminationPolicy }}
    termination: {{ .Values.hr.route.tls.termination }}
  to:
    kind: Service
    name: {{ .Values.hr.service.name }}
    weight: 100
{{- end }}
