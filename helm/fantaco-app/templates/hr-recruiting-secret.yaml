{{- if .Values.hrRecruiting.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: fantaco-hr-recruiting-secret
type: Opaque
stringData:
  database.password: {{ .Values.hrRecruiting.database.password | quote }}
{{- end }}
