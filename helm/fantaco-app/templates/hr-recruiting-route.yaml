{{- if and .Values.hrRecruiting.enabled .Values.hrRecruiting.route.enabled }}
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ .Values.hrRecruiting.route.name }}
  labels:
    app: {{ .Values.hrRecruiting.name }}
spec:
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: {{ .Values.hrRecruiting.route.tls.insecureEdgeTerminationPolicy }}
    termination: {{ .Values.hrRecruiting.route.tls.termination }}
  to:
    kind: Service
    name: {{ .Values.hrRecruiting.service.name }}
    weight: 100
{{- end }}
