{{- if .Values.finance.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.finance.name }}
  labels:
    app: {{ .Values.finance.name }}
spec:
  replicas: {{ .Values.finance.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.finance.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.finance.name }}
    spec:
      containers:
      - name: mcp-server
        image: {{ .Values.finance.image.repository }}:{{ .Values.finance.image.tag }}
        imagePullPolicy: {{ .Values.finance.image.pullPolicy }}
        env:
        - name: FINANCE_API_BASE_URL
          value: {{ .Values.finance.env.financeApiBaseUrl | quote }}
        - name: OTEL_SERVICE_NAME
          value: "fantaco-finance-mcp"
        - name: OTEL_EXPORTER_OTLP_TRACES_ENDPOINT
          value: {{ .Values.otel.endpoint | quote }}
        - name: OTEL_EXPORTER_OTLP_PROTOCOL
          value: {{ .Values.otel.protocol | quote }}
        - name: OTEL_TRACES_EXPORTER
          value: {{ .Values.otel.exporter | quote }}
        ports:
        - containerPort: {{ .Values.finance.service.port }}
          name: http
          protocol: TCP
        resources:
          requests:
            memory: {{ .Values.finance.resources.requests.memory }}
            cpu: {{ .Values.finance.resources.requests.cpu }}
          limits:
            memory: {{ .Values.finance.resources.limits.memory }}
            cpu: {{ .Values.finance.resources.limits.cpu }}
{{- end }}
